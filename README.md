# Ящик

Выезжающая панель для окон Windows. Хоткей выдвигает окно приложения
сбоку экрана поверх остальных, повторный хоткей убирает его за край.
Окно не закрывается и не сворачивается — состояние сохраняется.

Написано после того, как WTQ на этой машине оказался нерабочим:
он падал на свёрнутых окнах, зависал на служебных окнах PhpStorm,
а зависший экземпляр блокировал все последующие запуски.

## Запуск

Нужен AutoHotkey v2. Запускается двойным щелчком по `src\drawer.ahk`
или ярлыком на рабочем столе.

| Хоткей | Действие |
|---|---|
| `Ctrl+Alt+1` | VS Code |
| `Ctrl+Alt+2` | PhpStorm |
| `Ctrl+Alt+3` | временный ящик: запомнить активное окно и управлять им |
| `Ctrl+Alt+Shift+1…3` | вернуть фокус в соответствующий ящик |
| `Ctrl+Alt+0` | выход, окна возвращаются на исходные места |

Хоткеи задаются в настройках, таблица отражает значения по умолчанию.

Второй запуск не конфликтует с первым: новый экземпляр вытесняет старый.
При запуске программа показывает уведомление «Ящик запущен, хоткеев
живо: N» — если уведомления нет или число меньше ожидаемого, значит
хоткеи не встали.

Хоткеи ставятся через клавиатурный хук, а не `RegisterHotkey`: иначе
сочетание может молча остаться за не успевшим умереть экземпляром, и
программа работает без клавиш.

## Настройка

Всё в начале `src\drawer.ahk`, в блоке `НАСТРОЙКИ`. Массив `apps` задаёт
приложения, по одной записи на приложение:

| Поле | Значение |
|---|---|
| `exe` | имя процесса, например `Code.exe` |
| `cls` | класс окна; `""` если не важен |
| `hotkey` | хоткей в синтаксисе AutoHotkey: `^` Ctrl, `!` Alt, `+` Shift, `#` Win |
| `focusHotkey` | вернуть фокус: выдвинуть, если спрятано, и активировать; `""` выключает |
| `monitor` | `"cursor"` — монитор под курсором, либо номер: `1`, `2`, `3`… |
| `edge` | `"left"`, `"right"`, `"top"`, `"bottom"` |
| `width` | размер панели в процентах рабочей области монитора: для `left`/`right` — ширина, для `top`/`bottom` — высота |
| `activateOnShow` | делать окно активным при выезде |
| `hideOnBlur` | убирать панель, когда фокус ушёл в другое окно |

Блок `temporary` задаёт временный ящик теми же полями, кроме `exe` и
`cls`: окно он берёт не по процессу, а то, что активно в момент нажатия.
`exitHotkey` задаёт хоткей выхода, `animMs` и `animSteps` — анимацию,
`animSteps := 0` выключает её.

После правки перезапустить скрипт.

## Поведение панели

`activateOnShow: true` — панель выезжает и забирает фокус.
`activateOnShow: false` — панель выезжает поверх остальных окон, но
работать вы продолжаете там, где работали.

`hideOnBlur: true` — панель уезжает сама, как только фокус ушёл в другое
окно. `false` — остаётся, пока не уберёте хоткеем.

Пара `activateOnShow: false` + `hideOnBlur: true` смысла не имеет: окно,
которому не давали фокуса, потерять его не может — слежение в этом
случае не включается.

Слежение за фокусом — таймер на 250 мс, и он работает только пока
выдвинуто окно с `hideOnBlur`. Когда все панели убраны, программа не
делает ничего.

`focusHotkey` нужен потому, что основной хоткей — переключатель: если
панель выдвинута, но вы ушли в другое окно, основной хоткей её уберёт,
а `Ctrl+Alt+Shift+N` вернёт вас в неё.

## Временный ящик

`Ctrl+Alt+3` делает ящиком то окно, которое активно прямо сейчас, и
дальше управляет **только им**: переход в другое приложение и курсор на
другом мониторе выбор не меняют. Запоминается конкретное окно, а не
приложение — из пяти окон Chrome ящиком станет ровно то, что было
активно при запоминании.

Привязка живёт, пока работает программа: после перезапуска Ящика окно
надо запомнить заново. Если запомненное окно закрыть, следующее нажатие
сбросит привязку и скажет об этом уведомлением, а ещё одно — запомнит
новое активное окно.

Рабочий стол и панель задач в ящик не берутся.

## Край, монитор и парковка

Край берётся из настройки как есть: программа не переворачивает `right`
в `left`, даже если справа стоит другой монитор. Монитор берётся из
настройки — под курсором или по номеру. Число мониторов, их расположение
и разрешения нигде не предполагаются: геометрия каждый раз запрашивается
у Windows.

Спрятанное окно паркуется за границей всего виртуального рабочего
стола, поэтому спрятать его за любой край можно всегда. Намеренно не
используется `WinHide`: скрытое им окно после аварийного завершения
программы вернуть штатными средствами Windows нельзя, а припаркованное —
находится и возвращается при следующем запуске.

Анимация уводит окно за край монитора. Если к выбранному краю примыкает
другой монитор, окно проехало бы по нему на виду — в этом случае
анимация пропускается и окно паркуется сразу.

## Чек-лист приёмки

Составлен по отказам, которые дал WTQ. Полный список — в
`docs/04-чек-лист-приёмки.md`.

| Сценарий | Результат |
|---|---|
| Второй экземпляр не блокирует запуск | пройден |
| Свёрнутое окно разворачивается, программа не падает | пройден |
| Окна без заголовка не захватываются | пройден |
| Приложение не запущено — уведомление, без падения | пройден |
| Выход возвращает окна на исходные места | пройден |
| Настройки не теряются | пройден, это константы в файле |
| Работа после перезагрузки | не проверен |
| Край не переворачивается программой | не проверен после ревизии |
| Монитор по номеру и отключённый монитор | не проверен |
| Перезапуск при спрятанном окне | не проверен |

## Известные ограничения

- При нескольких окнах одного приложения постоянный бинд берёт окно
  наибольшей площади. Временный ящик этим не страдает: он помнит
  конкретное окно.
- Временный ящик один, и он забывается при перезапуске программы.
- После того как панель убрана хоткеем, фокус остаётся на самом
  припаркованном окне — оно невидимо, но продолжает получать ввод.
- Щелчок по значку приложения на панели задач припаркованное окно не
  возвращает: Windows активирует его прямо за границей экрана. Разбор —
  в `docs/03-решения.md`, Р15.
- Если к выбранному краю примыкает другой монитор, выезд и уезд без
  анимации, рывком.
- Края `top` и `bottom` реализованы, но вживую не проверялись.
- Мониторы с разным масштабом (DPI) не проверялись.
- Автозапуска нет: программа запускается вручную.
- Окно остаётся в Alt+Tab и на панели задач.

## Документы

| Файл | О чём |
|---|---|
| `docs/01-бриф.md` | задача, пользователь, критерии успеха |
| `docs/02-требования.md` | требования с приоритетами v1 и «потом» |
| `docs/03-решения.md` | принятые решения и причины каждого |
| `docs/04-чек-лист-приёмки.md` | сценарии проверки и замеры прогона |
| `docs/05-план-работ.md` | что сделано и что дальше, по этапам |
| `docs/06-почему-не-wtq.md` | отказы предшественника с выдержками из логов |

## Что дальше

Отложено до того, как база покажет себя в работе:
убрать окно из Alt+Tab, прятать при потере фокуса,
захват любого активного окна по хоткею.
