# Drawer — Product & Behavior Specification

**Статус:** DRAFT — требует редакции владельца  
**Эталон поведения:** `6bfa010fbf0ca7e1b47e856b7c13a450ff54b1fa`  
**Владелец продукта:** пользователь

Этот документ описывает **что должен делать Drawer**, а не как это реализовано. Код, тесты и архитектурные решения не должны менять описанное здесь поведение без явного решения владельца.

> ⚠️ `USER REVIEW` — место, которое нужно подтвердить или поправить.

## 1. Что такое Drawer

Drawer держит выбранные окна приложений в «карманах» за краем экрана и позволяет быстро выдвигать/прятать их хоткеем или через кромку. Окно не закрывается и не сворачивается, его рабочее состояние сохраняется.

Основной сценарий: VS Code / PhpStorm / браузер / терминал доступны напрямую, без поиска через Alt+Tab.

## 2. Слоты и хоткеи

Есть 9 слотов: `1…9`.

- `Ctrl+Alt+N` — показать/спрятать окно слота N.
- `Ctrl+Alt+Shift+N` — назначить активное окно в динамический слот N.
- Постоянный слот задаётся `[slotN]` в настройках и динамически не перезаписывается.
- Динамическая привязка живёт до закрытия окна, очистки или перезапуска Drawer.
- `Ctrl+Alt+0` — очистить динамические привязки. ⚠️ USER REVIEW
- `focusHotkey` — показать окно при необходимости и передать ему фокус.

## 3. Показ и скрытие окна

При показе окно выезжает из настроенного края (`left/right/top/bottom`) на заданную долю рабочей области монитора.

При скрытии окно уезжает за край, но остаётся живым. Повторный показ возвращает то же окно с его состоянием.

- `animMs` / `animSteps` управляют анимацией.
- Если за выбранным краем непосредственно расположен другой монитор, окно скрывается/показывается без проезда через соседний экран. ⚠️ USER REVIEW
- `activateOnShow=true` — окно получает фокус.
- `hideOnBlur=true` — после ухода фокуса окно автоматически прячется.
- При скрытии активного Drawer-окна фокус должен вернуться предыдущему рабочему окну.
- Активация спрятанного Drawer-окна через Windows должна вернуть его на экран.

## 4. Мониторы — НЕ МЕНЯТЬ БЕЗ РЕШЕНИЯ ВЛАДЕЛЬЦА

`monitor=cursor` — **динамический режим**.

- Кромка следует за монитором, на котором сейчас находится курсор.
- Управляемое окно следует за монитором курсора.
- При Show используется **текущий** монитор курсора.
- Монитор первоначального bind не закрепляется за окном.
- Перевёл курсор на другой монитор → следующий показ/кромка работают на этом мониторе.

`monitor=1/2/3…` — используется явно выбранный монитор. ⚠️ USER REVIEW поведения при отключении этого монитора.

Количество, расположение и разрешение мониторов заранее не предполагаются; геометрия берётся у Windows.

## 5. Кромки

Кромка — маленькая плитка у края экрана для припаркованного окна.

- Показывает иконку приложения.
- При наведении увеличивается и показывает номер слота.
- Клик действует как `Ctrl+Alt+N`.
- Кромка должна оставаться доступной поверх обычных/развёрнутых окон. ⚠️ USER REVIEW
- Для `monitor=cursor` кромка перемещается между мониторами вслед за курсором вместе с соответствующим поведением окна.
- `handles=false` полностью отключает кромки и связанный с ними опрос мыши.

### Визуальный эталон

⚠️ **USER REVIEW / SCREENSHOT:** вставить утверждённый скриншот кромки.

## 6. Постоянные и динамические приложения

### Постоянный слот

Привязан к приложению через настройки (`exe`, при необходимости `cls`) и сохраняется после перезапуска Drawer.

### Динамический слот

Привязывается к текущему активному окну через `Ctrl+Alt+Shift+N`. Использует общие `[dynamic]` настройки с возможными overrides `[dynamicSlotN]`.

⚠️ USER REVIEW: точное ожидаемое UX кнопок «Закрепить приложение» / «Сделать временным» в Settings нужно описать после проверки интерфейса baseline.

## 7. Settings

Settings — графическая оболочка над `config.ini`, а не отдельное хранилище.

Пользователь настраивает:

- длительность/шаги анимации;
- blur interval;
- кромки;
- постоянные слоты;
- приложение/имя слота;
- monitor;
- edge;
- размер панели;
- `activateOnShow`;
- `hideOnBlur`;
- `focusHotkey`;
- настройки динамических слотов / overrides. ⚠️ USER REVIEW состава текущего UI

Изменения записываются только по явному Apply/OK; `config.ini` остаётся постоянным источником настроек.

### Визуальный эталон

⚠️ **USER REVIEW / SCREENSHOTS:** добавить утверждённые экраны Settings и подписать назначение спорных элементов.

## 8. Tray и жизненный цикл

- Drawer запускается один раз и работает в фоне весь день.
- Второй экземпляр вытесняет старый, а не оставляет два конфликтующих процесса.
- Явный Exit должен вернуть управляемые окна в нормальное доступное состояние.
- Ошибка/закрытие одного управляемого окна не должна валить Drawer.

⚠️ USER REVIEW: перечислить точные пункты tray-меню и их действия.

## 9. Что считается регрессией

Без явного решения владельца нельзя:

- закреплять `monitor=cursor` за монитором первоначального bind;
- менять направление/характер Show/Hide;
- позволять анимации визуально проезжать через соседний монитор;
- делать кромку недоступной из-за обычного развёрнутого окна;
- менять смысл существующих хоткеев;
- менять постоянный слот на динамический или наоборот автоматически;
- менять Settings/config semantics;
- считать зелёный unit/seam test достаточным основанием для изменения пользовательского поведения.

## 10. Приёмка изменений

1. Сравнивать поведение с этим документом и baseline `6bfa010`.
2. Любая неоднозначность поведения → вопрос владельцу, а не новое архитектурное решение.
3. Runtime-sensitive изменение → короткая ручная проверка пользователем до следующего такого изменения.
4. Рефакторинг должен быть behavior-neutral: если поведение изменилось, это не «просто рефакторинг».

## Что нужно отредактировать владельцу

Всего пять вещей:

1. подтвердить/поправить помеченные `USER REVIEW` места;
2. уточнить поведение кнопок «Закрепить приложение» / «Сделать временным»;
3. уточнить tray;
4. приложить утверждённые скриншоты кромки и Settings;
5. дописать любой привычный сценарий, который здесь пропущен.
