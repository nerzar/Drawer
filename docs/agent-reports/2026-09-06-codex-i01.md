# I01 — C01 + G01 integration

Canonical branch: `integration/slots-settings-wave1`.

Integrated reviewed C01 (`e4f5271`) and G01 (`8d01da3`) on the current
orchestration base. The resulting `drawer.ahk` retains C01's persisted
show/hide hotkey registry, live rebind, dynamic lifecycle, and late permanent
discovery while also retaining G01's DWM dark-titlebar calls and build safety.

Integration review fixes:

- permanent-slot `Имя` is editable;
- WebView hotkey capture leaves Tab to normal form navigation and does not
  replace the hotkey;
- the WebView smoke asserts both behaviours.

Verification:

- AutoHotkey `/validate`;
- `test/narrow/settings-seam.ahk`;
- `test/narrow/webview-slice.ps1`;
- frontend `npm test`, `npm run typecheck`, and `npm run build`;

Production build could not be verified in the isolated worktree: the copied
Ahk2Exe returned exit code 17 while compiling `src/drawer.ahk`. The source
validation and all required narrow checks completed successfully.

VM/full suite was intentionally not run.
