# G05 — Slots UX terminology/onboarding/narrow layout

- Task ID: `G05`
- Run ID: `RUN-20260906-CODEX-G05-IMPLEMENT-01`
- Agent/client: `Codex`
- Model: `GPT-5, economical configuration; exact client variant NOT_EXPOSED`
- Chat/session ID: `NOT_EXPOSED`
- Chat title: `NOT_EXPOSED`
- Search anchor: `Drawer G05 temporary slots UX — Codex 20260906`
- Started at: `NOT_EXPOSED`
- Finished at: `2026-09-06T14:34:27+03:00`
- Worktree: `C:\Users\nerza\Projects\drawer-agent-worktrees\G05`
- Branch: `fix/slots-ux-terminology-layout`
- Base SHA: `c1e710345543c3eb7ff3cb2a9d3a6f13c57ff297`
- Code SHA: `bb385fa63294addcdbb82fbbcd900c2258ce50ff`
- Report tip SHA: `PENDING_FINAL_COMMIT`
- Remote: `dev`

## 1. Goal

Make temporary-slot binding discoverable, remove internal terminology from rendered Slots copy, distinguish window release from behavior reset, and keep the reset controls usable in the narrow detail pane without changing slot semantics.

## 2. Result

- Empty temporary slots explain the fixed `Ctrl + Alt + Shift + N` binding workflow and show the slot's current show/hide hotkey.
- User-facing mode and action copy now uses `Временный`, `Закрепить за приложением…`, `Сделать временным…`, `Отвязать окно`, and `Вернуть общие настройки`.
- Temporary slots no longer render fake name/executable identity rows; bound and empty states receive distinct lifetime guidance.
- Rendered copy no longer exposes `[dynamic]`, `[dynamicSlotN]`, `[slot N]`, `ahk_class`, or mixed-language `show/hide` wording.
- Reset feedback states that only parameters change and the window remains bound; a staged reset is explained as pending Apply.
- The reset block removes the inherited 120 px `.hotkey-cap` margin, wraps, and allows the reset button text and header actions to wrap.
- Backend behavior, bridge contracts, AHK files, and `src/config.ini` were not changed.

## 3. Commits

- `bb385fa63294addcdbb82fbbcd900c2258ce50ff` — `fix(settings): clarify temporary slot UX`

## 4. Important decisions

- Existing Release, Reset, conversion methods, and `data-testid` contracts were preserved.
- The onboarding displays the draft hotkey when present, so a customized show/hide shortcut is not mislabeled as the default `Ctrl + Alt + N`.
- A focused source-level regression test was added without changing the shared npm test entrypoint or adding a DOM framework.

## 5. Problems found

- The generic `.hotkey-cap` rule applied a 120 px left margin inside the narrow reset box.
- Existing copy implied temporary slots lacked their own hotkey and exposed storage/runtime vocabulary.

## 6. Tests / verification

- Focused `slotsUx.test.ts`: 3/3 passed.
- `npm --prefix settings-ui test`: 47/47 passed.
- `npm --prefix settings-ui run typecheck`: passed.
- `npm --prefix settings-ui run build`: passed.
- `git diff --check`: passed.
- AHK checks were not run because no AHK/runtime/bridge files changed.

## 7. Known issues / unfinished

- Native WebView manual acceptance at the narrow window width was not performed in this run; CSS behavior is covered by focused source assertions and the production frontend build.

## 8. Suggested next step

Review Code SHA `bb385fa63294addcdbb82fbbcd900c2258ce50ff`, then manually open an empty temporary slot at the narrow Settings width and confirm the onboarding and reset button remain fully visible.
