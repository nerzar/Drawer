# RUN-20260908-ANTIGRAVITY-DWM-THUMBNAIL-POC-01

Status: READY
Agent: ANTIGRAVITY
Session: NEW
Code SHA: `38c6daa586c86394733a09f1968c4de3f6351e13`
Task branch: `poc/dwm-thumbnail-animation-antigravity`

## Цель

Проверить отдельным маленьким прототипом, годится ли Windows DWM Thumbnail как визуальный animation layer для Drawer: настоящее окно не должно проезжать через соседний монитор, а его визуальное представление должно уметь плавно появляться/двигаться только внутри целевого монитора.

Это исследование, не production-изменение.

## Что сделать

1. Начать от указанного Code SHA, fresh fetch перед работой.
2. Не менять production-поведение Drawer и не трогать `src/drawer.ahk`, `src/Slots.ahk` или Settings.
3. Сделать минимальный standalone PoC в отдельной test/experiments области репозитория. Допустим AHK/Win32 через `DwmRegisterThumbnail` / `DwmUpdateThumbnailProperties`.
4. Источник — обычное тестовое окно (например Notepad). Реальное source-окно во время анимации должно оставаться в фиксированном безопасном положении.
5. На конфигурации с двумя соседними мониторами показать thumbnail/overlay так, чтобы вся визуальная анимация происходила внутри выбранного целевого монитора и не появлялась на соседнем.
6. Проверить хотя бы slide/reveal-подобное движение. Fade/zoom не нужны, если они не получаются почти бесплатно из того же PoC.
7. Runtime-проверка только в подготовленной VM. На основной Windows пользователя ничего не запускать.
8. Если VM не работает без существенного ремонта — остановиться с BLOCKED и описать конкретную проблему; не переносить runtime-тесты на host.

## Результат

В report указать:

- удалось ли DWM Thumbnail технически использовать для этой задачи;
- что происходит с source-окном и thumbnail во время анимации;
- есть ли мерцание, задержка, артефакты, проблемы с DPI/двумя мониторами;
- какие ограничения DWM критичны для Drawer;
- короткий вывод: `PROMISING`, `NOT_SUITABLE` или `NEEDS_FOLLOWUP`;
- точный commit/branch PoC;
- какую визуальную проверку реально удалось провести в VM.

Не интегрировать PoC в Drawer и не промотить код. После завершения обновить claim/report по обычным правилам и снова прочитать `AGENT_BOARD.md`.