# RUN-20260908-GEMINI-SETTINGS-PICKERS-VM-01

- Run ID: `RUN-20260908-GEMINI-SETTINGS-PICKERS-VM-01`
- Agent: `GEMINI`
- Status: `READY`
- Session: `NEW`
- Base SHA: `c68929c6adf2a6b7291af4b8c829b863c00754ca`
- Source branch: `codex/fix-settings-open`

## Цель

В готовой одномониторной Windows VM проверить выбор приложения и окна в WebView2 Settings, включая старый риск сохранения `windowClass` от прежнего приложения.

## Scope

1. Сделать fresh fetch и проверить, что тестируется ровно `c68929c6adf2a6b7291af4b8c829b863c00754ca` с собранным `settings-ui`.
2. Использовать отдельный тестовый `config.ini` и два приложения с различающимися exe/class.
3. Подготовить постоянный слот с первым exe и непустым `windowClass`; через picker приложения выбрать второй exe, применить изменения и проверить, что старый class не остался в UI или `[slotN]`.
4. Через picker окна выбрать живое окно другого приложения, применить изменения и проверить согласованную пару exe/class в UI и config.
5. Для обоих сценариев закрыть и снова открыть Settings, затем убедиться, что Drawer находит ожидаемое приложение и slot show/hide работает.
6. Проверить отмену picker: cancel не должен менять draft или config.

## Ограничения

- Только runtime/GUI-проверка; production-код и документы не менять.
- Не проверять конверсию типа, «Сбросить слот», full reset и multi-monitor.
- Не расширять проверку до общего аудита Settings.
- Использовать готовую VM. Если мешает инфраструктура, вернуть конкретный blocker; не смешивать её ремонт с продуктовой проверкой.

## Ожидаемый результат

Вернуть Code SHA, точные шаги, config до/после, релевантный debug log и скриншоты. Итог отдельно для picker приложения, picker окна и cancel: `PASS`/`FAIL`. При `FAIL` указать, где возник stale или несогласованное значение, и не исправлять код.
