# RUN-20260908-MUSE-FULL-RESET-VM-01

- Run ID: `RUN-20260908-MUSE-FULL-RESET-VM-01`
- Agent: `MUSE`
- Status: `READY`
- Session: `NEW`
- Base SHA: `12c45e4a7fb59642e42318f4b8a8ef4b4db0343b`
- Source branch: `codex/full-reset-hotkey`

## Цель

В готовой одномониторной Windows VM подтвердить, что физический `Ctrl+Alt+Shift+0` выполняет полный сброс по `PRODUCT_SPEC.md`, а не завершает Drawer.

## Scope

1. Сделать fresh fetch и проверить, что тестируется ровно `12c45e4a7fb59642e42318f4b8a8ef4b4db0343b`.
2. Использовать отдельную копию `src` и отдельный тестовый `config.ini`, чтобы не затронуть настройки владельца.
3. До запуска задать отличающиеся от defaults общие настройки, `[dynamic]`, хотя бы один `[slotN]`, `[dynamicSlotN]` и изменённый slot hotkey.
4. Запустить Drawer в VM, создать хотя бы одну динамическую привязку и нажать физический `Ctrl+Alt+Shift+0`.
5. Проверить после сброса:
   - процесс Drawer жив и хоткеи продолжают работать;
   - постоянные и динамические привязки отсутствуют;
   - `[slotN]`, `[dynamicSlotN]` и пользовательские `[hotkeys]` удалены;
   - общие настройки и defaults слотов вернулись к штатным значениям;
   - WebView2 Settings открываются и показывают уже сброшенное состояние;
   - debug log содержит нажатие full reset и завершение операции, без `Exit Drawer` и необработанной ошибки.

## Ограничения

- Только runtime/GUI-проверка; production-код и документы не менять.
- Использовать уже проверенный `drawer-vm` workflow. VMware/GUI tooling внутри этой задачи не разрабатывать и не чинить; инфраструктурный blocker вернуть для `GEMINI`.
- Не тестировать на основной Windows владельца и не добавлять multi-monitor сценарий.
- Не расширять проверку до общего regression-прогона.

## Ожидаемый результат

Обновить этот файл: `Status: DONE` при полном прохождении либо `Status: BLOCKED`/`FAILED` с точным шагом. Приложить Code SHA, config до/после, релевантный фрагмент debug log и короткий итог `PASS`/`FAIL`. При `FAIL` вернуть наблюдаемый runtime-факт; исправление кода не делать.
