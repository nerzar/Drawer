# G03FIX — correct watcher authority regressions found by independent review

- Run ID: `RUN-20260906-CODEX-G03FIX-01`
- Executor: Codex
- Model: economical/older coding model; do not switch to heavy model unless genuinely blocked
- Canonical G03 Code SHA to fix: `be028a0b10af88040c79f7faa9bd04219288b256`
- Review input: `review/g03-live-settings@d3da6ee1be2e270429b26957af134099eb31d1b0`
- Output branch: `fix/settings-live-blur-watch-authority`

## Goal
Fix the two correctness regressions identified by G03R while preserving the intended G03 live hideOnBlur/blurMs reconciliation and C03 save semantics.

## Required fixes
1. Preserve watcher eligibility for `activateOnShow=false`:
   - normal Show with `activateOnShow=false` must not become newly watched merely because Settings Apply/reconcile ran;
   - a legitimate watcher created through `FocusWindow()` must remain legitimate when appropriate;
   - do not solve this by blindly requiring `activateOnShow=true` for every watched HWND.
2. For duplicate HWND represented by permanent + dynamic/temporary slots, watcher behavior must use the same authoritative precedence as `Slots.SlotOf(hwnd)`: permanent binding wins.
3. If straightforward, remove compounded disabled opacity in GeneralView so save-lock dims once rather than fieldset + descendants. This is LOW severity and must not expand scope.

## Tests
Add focused regression coverage for at least:
- deployed/non-activated window not newly enrolled after unrelated Apply;
- FocusWindow-created legitimate watcher preserved where expected;
- duplicate HWND conflicting hideOnBlur uses permanent-slot precedence;
- one watcher becoming ineligible does not disturb another valid watcher;
- stale watcher cleanup remains correct.
Prefer tests that exercise production authority/resolution logic rather than another independent model copy. If architecture makes direct seam execution impractical, document the limitation honestly and strengthen the narrowest available seam.

Run AHK validate + relevant narrow seam, settings-ui tests/typecheck/build if frontend touched. Preserve `src/config.ini`.

## Git
Start from canonical G03 Code SHA `be028a0...`, not a report-tip SHA. Dedicated sibling worktree. Read G03R report before changes. Push output branch to `dev`. Report per REPORT_FORMAT with separate Code SHA / Report tip SHA. Do not edit AGENT_BOARD or ARCHITECT_STATE.

Final answer: Run ID, DONE/BLOCKED, branch, Code SHA, report tip SHA, tests, concise explanation of watcher-authority fix.