# G05FIX — fix two UX correctness issues found by G05 review

- Run ID: `RUN-20260906-CODEX-G05FIX-01`
- Executor: Codex
- Model: economical/older coding model; do not use a heavy model unless genuinely blocked
- Base Code SHA: `bb385fa63294addcdbb82fbbcd900c2258ce50ff` (accepted G05 implementation code)
- Review input: latest `review/g05-slots-ux` report from DEV-1
- Output branch: `fix/slots-ux-state-truth`

## Goal
Fix the two user-visible state-truth bugs identified by G05 review without changing backend slot semantics.

## Required fixes
1. Temporary-slot onboarding must not present a hotkey as active when it is disabled or only staged in draft before Apply. Render only canonical/effective active hotkey truth; if draft differs, make pending state explicit instead of presenting it as already active.
2. During draft-only permanent -> temporary conversion, UI must not claim the runtime window is already temporarily bound and must not expose `Отвязать окно` as if conversion had already been applied. Pending conversion should be visibly staged until Apply.

## Constraints
- Prefer frontend-only fix in `SlotsView.vue` / existing slot draft helpers and focused tests.
- Do not alter backend/AHK/protocol/config semantics unless review proves frontend cannot express truth correctly; if so, BLOCK rather than expanding scope.
- Preserve G05 terminology/onboarding/narrow-layout improvements.
- Preserve A01FIX/G02/C03 accepted behavior.
- Do not touch `src/config.ini`.

## Verification
Add focused regressions for disabled hotkey, draft-hotkey-before-Apply, and pending permanent->temporary conversion/release visibility. Run `npm --prefix settings-ui test`, typecheck and build. AHK checks only if AHK/runtime files are touched.

## Completion
Use dedicated sibling worktree. Report per REPORT_FORMAT with separate Code SHA and Report tip SHA. Push branch to `dev`, verify remote ref, clean tree. Final: Run ID, DONE/BLOCKED, branch, Code SHA, report tip SHA, checks, concise summary.