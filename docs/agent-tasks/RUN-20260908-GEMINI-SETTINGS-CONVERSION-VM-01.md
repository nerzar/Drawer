# RUN-20260908-GEMINI-SETTINGS-CONVERSION-VM-01

- Run ID: `RUN-20260908-GEMINI-SETTINGS-CONVERSION-VM-01`
- Agent: `GEMINI`
- Status: `READY`
- Session: `NEW`
- Base SHA: `c68929c6adf2a6b7291af4b8c829b863c00754ca`
- Source branch: `codex/fix-settings-open`

## Цель

В готовой одномониторной Windows VM проверить фактическую работу WebView2 Settings при конверсии слота «Сделать постоянным» и «Сделать временным».

## Scope

1. Сделать fresh fetch и проверить, что тестируется ровно `c68929c6adf2a6b7291af4b8c829b863c00754ca` с собранным `settings-ui`.
2. Использовать отдельный тестовый `config.ini` и два простых тестовых приложения/окна.
3. Привязать окно к динамическому слоту, выполнить «Сделать постоянным», применить изменения и проверить UI, runtime и `[slotN]` в файле.
4. Для отдельного постоянного слота выполнить «Сделать временным», применить изменения и проверить UI, runtime и удаление `[slotN]`.
5. После каждого перехода проверить show/hide хоткей, закрыть и снова открыть Settings и убедиться, что старый draft не возвращает прежний тип.
6. Зафиксировать, остаётся ли текущее найденное окно привязанным после каждого перехода; если это расходится между UI, runtime и config, вернуть точный runtime-факт.

## Ограничения

- Только runtime/GUI-проверка; production-код и документы не менять.
- Не проверять «Сбросить слот», full reset, picker приложения/окна и multi-monitor.
- Не принимать новое продуктовое решение о lifecycle окна. Неоднозначное, но согласованное между UI/runtime/config поведение описать владельцу без оценки как бага.
- Использовать готовую VM. Если мешает инфраструктура, вернуть конкретный blocker; не смешивать её ремонт с продуктовой проверкой.

## Ожидаемый результат

Вернуть Code SHA, точные шаги, config до/после, релевантные строки debug log и скриншоты двух переходов. Итог отдельно для dynamic → permanent и permanent → dynamic: `PASS`, `FAIL` или `OWNER_DECISION_NEEDED`. При `FAIL` не исправлять код.
