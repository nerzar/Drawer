# RUN-20260908-MUSE-SETTINGS-SLOT-RESET-VM-01

- Run ID: `RUN-20260908-MUSE-SETTINGS-SLOT-RESET-VM-01`
- Agent: `MUSE`
- Status: `READY`
- Session: `NEW`
- Base SHA: `c68929c6adf2a6b7291af4b8c829b863c00754ca`
- Source branch: `codex/fix-settings-open`

## Цель

В готовой одномониторной Windows VM проверить одну утверждённую операцию WebView2 Settings: «Сбросить слот» удаляет приложение/текущую привязку и возвращает настройки только этого слота к defaults.

## Scope

1. Сделать fresh fetch и проверить, что тестируется ровно `c68929c6adf2a6b7291af4b8c829b863c00754ca` с собранным `settings-ui`.
2. Использовать отдельный тестовый `config.ini`.
3. Для постоянного слота задать приложение и индивидуальные настройки, выполнить «Сбросить слот» и применить изменения.
4. Для другого динамического слота создать runtime-привязку и индивидуальные overrides, выполнить ту же операцию и применить изменения.
5. В каждом случае проверить UI, runtime и файл:
   - приложение/окно больше не принадлежит сброшенному слоту;
   - индивидуальные настройки сброшенного слота вернулись к `[dynamic]` defaults;
   - соответствующие `[slotN]`/`[dynamicSlotN]` удалены;
   - соседний контрольный слот не изменился;
   - после закрытия и повторного открытия Settings состояние не восстанавливается из старого draft.

## Ограничения

- Только runtime/GUI-проверка; production-код и документы не менять.
- Не проверять одновременно конверсию постоянный ↔ временный, full reset и multi-monitor.
- Использовать уже проверенный `drawer-vm` workflow. VMware/GUI tooling внутри задачи не чинить; инфраструктурный blocker вернуть для `GEMINI`.
- Не тестировать на основной Windows владельца.

## Ожидаемый результат

Обновить этот файл: `Status: DONE` при полном прохождении либо `Status: BLOCKED`/`FAILED`. Приложить Code SHA, краткие шаги, config до/после, релевантный debug log и итог отдельно для permanent и dynamic. При дефекте вернуть минимальный воспроизводимый runtime-факт; исправление кода не делать.
